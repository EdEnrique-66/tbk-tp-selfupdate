// src/setupTests.js

// Si usas @testing-library/jest-dom para expectativas como .toBeInTheDocument()
import '@testing-library/jest-dom';

// Puedes agregar otras configuraciones globales aquí.
// Por ejemplo, mocks de APIs o variables de entorno.
// global.console.log = vi.fn();